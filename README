This project is to help me keep my vim setup working across all my machines.

On a new machine, here are the commands to get going:
  cd ~
  git clone git://github.com/brisketeer/dotvim.git ~/.vim
  ln -s ~/.vim/vimrc ~/.vimrc
  
  cd ~/.vim
  git submodule update --init
